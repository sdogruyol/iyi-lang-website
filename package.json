{
  "name": "iyi-site",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/fonts.mjs && node scripts/facts.mjs && node scripts/targets.mjs && node scripts/proofs.mjs && node scripts/releases.mjs && node scripts/install-targets.mjs && node scripts/samples.mjs && node scripts/thesis.mjs && node scripts/project.mjs && node scripts/download.mjs && node scripts/records.mjs && node scripts/contrast.mjs && node scripts/no-transcription.mjs && node scripts/blocker.mjs",
    "generate:install-targets": "node scripts/install-targets.mjs",
    "record:wasm": "node scripts/record-wasm.mjs",
    "record:diagnostics": "node scripts/record-diagnostics.mjs",
    "record:highlight": "node scripts/record-highlight.mjs",
    "record:agent": "node scripts/record-agent.mjs",
    "record": "npm run record:wasm && npm run record:diagnostics && npm run record:agent && npm run record:highlight",
    "predev": "npm run generate",
    "dev": "astro dev",
    "prebuild": "npm run generate",
    "build": "astro build",
    "postbuild": "node scripts/prose.mjs && node scripts/links.mjs && node scripts/budget.mjs",
    "budget:adopt": "node scripts/budget.mjs --adopt",
    "preview": "astro preview",
    "precheck": "npm run generate",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.7",
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource-variable/figtree": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
