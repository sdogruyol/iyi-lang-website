---
/** The `###` of a lesson, mapped onto the anchored heading beside this file. */
import Heading from "./_Heading.astro";

const { id } = Astro.props;
---

<Heading level={3} id={id}><slot /></Heading>
